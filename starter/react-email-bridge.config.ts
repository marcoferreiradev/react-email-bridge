import { defineConfig } from 'react-email-bridge';

export default defineConfig({
  // outputExtension: '.html',  // change export extension (default .hbs)
  // strict: true,              // throw on missing variables (default false)
  // previewHelpers: {
  //   uppercase: (s) => String(s).toUpperCase(),
  // },
});

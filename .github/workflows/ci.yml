name: CI

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 10

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Typecheck (workspace root)
        run: pnpm typecheck

      - name: Validate pipeline (P0 stress test)
        run: pnpm validate

      - name: Build packages
        run: pnpm build

      - name: Unit tests
        run: pnpm test

      - name: Smoke export — generic-hbs example
        run: |
          cd examples/generic-hbs
          node ../../packages/react-email-bridge/dist/cli/index.mjs export --all
          test -f dist/order-summary.hbs
          grep -q "{{customer.firstName}}" dist/order-summary.hbs

      - name: Smoke export — vtex-store example
        run: |
          cd examples/vtex-store
          node ../../packages/react-email-bridge/dist/cli/index.mjs export --all
          test -f dist/order-confirmed.hbs
          test -f dist/order-invoiced.hbs
          grep -q "{{#richShippingData shippingData}}" dist/order-confirmed.hbs
          grep -q "{{#each ../../../../items}}" dist/order-invoiced.hbs

      - name: Smoke pack-all + new-project bootstrap (default + vtex-full)
        run: |
          pnpm pack-all
          # default starter
          node scripts/new-project.mjs /tmp/ci-default --skip-install
          test -f /tmp/ci-default/emails/welcome.tsx
          test -f /tmp/ci-default/vendor/react-email-bridge-0.0.1.tgz
          test -f /tmp/ci-default/vendor/react-email-bridge-ui-0.0.1.tgz
          # vtex-full starter
          node scripts/new-project.mjs /tmp/ci-vtex --template vtex-full --skip-install
          test -f /tmp/ci-vtex/package.json
          test -f /tmp/ci-vtex/vendor/react-email-bridge-0.0.1.tgz

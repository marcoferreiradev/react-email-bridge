---
---

feat(examples/vtex-store): Bloco 11 — theme tokens + Halo-style pilot

Adds the design-token theme (theme.ts) and redesigns order-confirmed as
the Halo-inspired pilot. Logo + Regards shared components also updated
to use theme tokens (every template that uses them inherits the new
visual baseline). Other 12 templates still render with their faithful-
port styles and modernize in PR M.2+.

No public API change.
